/**
 * Flag used to store preload init data
 */
export const K_PRELOAD_INIT_FLAG = "__preload_init";

/**
 * Flag used to check if our preload script is allowed to run
 */
export const K_PRELOAD_INIT_KEY = "allowed";

/**
 * Flag used to store the value of the preload-capture window title
 */
export const K_PRELOAD_WIN_KEY = "window-title";
